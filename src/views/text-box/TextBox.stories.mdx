import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks';
import { TextBox } from './TextBox';

<Meta title='Components/Text Box' component={TextBox} />

# Text Box

A standard text box, with configurable placeholder text.

export const Template = (args) => <TextBox {...args} />;

<Canvas>
  <Story
    name='Placeholder'
    args={{
      placeholder: 'This is a placeholder',
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name='No Placeholder'
    args={{
      placeholder: null,
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable for={TextBox} />
