import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks';
import { Button } from './Button';

<Meta title='Components/Button' component={Button} />

# Button

A standard button, with configurable text for demonstration.

export const Template = (args) => <Button {...args} />;

<Canvas>
  <Story
    name='Primary'
    args={{
      text: 'Primary text',
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name='Secondary'
    args={{
      text: 'Secondary alt text',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable for={Button} />
